using System.Collections.Generic;
using carPool.Context;
using System.Collections;
using System;
using System.Linq;

namespace carPool
{
    public static class Ridemaster
    {        
        public static List<Account> CreateRide()
        {
            List<Account> accounts = new List<Account>();                        
            

            return accounts;
        }

        public static Account RideAddition()
        {
     
     
        public static List<Account> BuildRide()
        {
            IEnumerable<Account> ride = Enumerable.Range(1, 10).Select(x => RideAddition());
            return ride.ToList();
        }

    }
}

